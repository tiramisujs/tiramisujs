export interface CacheAdapter {
	/** TODO: add logic */
	init: () => void;

	/** TODO: add logic for automatic relationships (similar to includes) */
	// get: (id: unknown, kind?: unknown, options?: unknown) => unknown;

	/** TODO: add logic for automatic relationships (similar to includes) */
	// set: (id: unknown, kind?: unknown, options?: unknown) => unknown;

	/** TODO: add logic */
	// delete: (id: unknown, kind?: unknown, options?: unknown) => unknown;
}
